# ProCount — תכנית תיקונים לביצוע

תאריך: 3 בספטמבר 2026. סטטוס: התכנית מוכנה; שלב 0 והתיקונים עדיין לא בוצעו.

מטרה: להשלים את 13 הסעיפים בענף תיקונים אחד, עם קומיט נפרד לכל באג/שיפור, בדיקת קבלה ותיעוד תוצאה. מזהי B01–B12 תואמים למספור רשימת המחקר המקורית; B13 הוא השלמת ערכי מאכל ב־AI מתוך מסך יצירת מאכל במאגר. סדר הביצוע נקבע לפי התלויות.

## כללי עבודה ומעקב

- כל קומיט תיקון מטפל במזהה B אחד בלבד וכולל את הקוד, הבדיקה והתיעוד הנדרשים לו. אין לאחד כמה באגים באותו קומיט או לבצע squash בין תיקונים.
- תשתית משותפת תיכנס בקומיט הראשון שזקוק לה; הבאים ישתמשו בה. בפרט: B04 מטפל בתוצאות ושגיאות שמירה, B06 בשליחה כפולה, B07 בהתאמה למאכל קיים.
- לאחר אימות כל תיקון לסמן את שורתו כהושלמה. מזהה הבאג בהודעת הקומיט יאפשר למצוא אותו ישירות ב־git log; במסירה יופיעו גם ה־hash ותוצאות הבדיקות.
- לשמור על JavaScript, שכבת הנתונים ב־store, תאריכים מקומיים והקטלוג המשותף. להשתמש בשדות, ברכיבים ובתלויות הקיימים.
- לבדוק מחדש את הממצאים לאחר הסנכרון: ייתכן שה־remote כבר כולל שינוי. אם סעיף כבר תוקן, לתעד את הקומיט הקיים ואת האימות במקום לייצר תיקון או קומיט ריק.

## שלב 0 — משיכת remote עדכני ויצירת ענף תיקונים

**תוצר:** ענף `fix/procount-food-flow-bugs` המבוסס על ברירת המחדל המעודכנת של origin, עם baseline מתועד לפני תיקון ראשון.

### 0.1 שמירת העבודה המקומית ובדיקת הבסיס

- [ ] לבדוק את מצב העץ וה־remote. בזמן כתיבת התכנית העץ נקי למעט מסמך זה, שאינו מנוהל עדיין ב־Git; הענף הפעיל הוא `feature/water-tracking-photo-flow`.
- [ ] לשמר את המסמך וכל שינוי מקומי אחר לפני מעבר ענף. אין לבצע reset, מחיקה, stash גורף או העתקה אוטומטית של שינויים לא קשורים.
- [ ] למשוך את מצב ה־remote ולברר את ענף ברירת המחדל החי. ההפניה המקומית שנבדקה היא `origin/master`; הפקודות בהמשך מותנות בכך שזה עדיין הבסיס הנכון.

```sh
git status --short
git remote -v
git fetch origin --prune
git ls-remote --symref origin HEAD
git log --oneline origin/master..HEAD
git diff --stat origin/master...HEAD
```

המחקר נערך בקומיט `c9db799`, בעוד ההפניה המקומית ל־origin/master בעת כתיבת התכנית היא `622a41e`. אלה נתונים מקומיים לפני fetch. אם קיימים שינויים שלא מוזגו בענף הקודם, לשמר אותו ולבחון את הקוד מחדש על הבסיס העדכני; אין לצרף אליו אוטומטית עבודת פיצ׳ר אחרת.

### 0.2 יצירת הענף מה־remote שנמשך

לאחר אימות הבסיס והיעדר התנגשויות עם העבודה המקומית:

```sh
git switch --no-track -c fix/procount-food-flow-bugs origin/master
git rev-parse HEAD origin/master
git status --short
```

- [ ] שני מזהי הקומיט ב־rev-parse זהים בנקודת ההתחלה. יצירה ישירות מ־origin/master לאחר fetch נותנת בסיס remote עדכני.
- [ ] אם הענף כבר קיים, לבדוק אם הוא שייך לסבב זה ולהמשיך ממנו; אין לדרוס אותו עם ‎-C. אם הוא שייך לעבודה אחרת, לבחור שם fix חדש ולתעדו.
- [ ] לשמור את המסמך בקומיט תיעוד נפרד: `docs: record practical ProCount bugfix plan`. קומיט זה אינו אחד מ־13 קומיטי התיקונים והשיפורים.

### 0.3 בדיקות בסיס אחרי הסנכרון

```sh
npm ci
npm test
npm run build
```

- [ ] לתעד את קומיט הבסיס, מספר הבדיקות, תוצאות הבנייה וכשלים קיימים. תוצאת המחקר הקודמת — 23/23 ובנייה תקינה — אינה תחליף לבדיקה אחרי fetch.
- [ ] לפתוח את היישום המקומי ולוודא שהמסלולים הרלוונטיים זמינים; להשתמש בנתוני בדיקה ובבקשות מדומות לבדיקות כתיבה.
- [ ] לבדוק זמינות Deno והרשאת קריאה ל־Supabase לצורך B12 ו־B13. חוסר בגישה חיה אינו מעכב תיקוני frontend, אך יירשם כחוסר אימות עד שיושלם.

## מפת הקומיטים — לבצע בסדר הזה

| סדר | מזהה | תיקון | הודעת קומיט מתוכננת | מצב |
|---|---|---|---|---|
| 01 | B08 | יצירת מאכל במאגר בלבד | `fix(B08): separate catalog creation from daily logging` | בוצע בקוד; אימות שמירה מול נתוני בדיקה ממתין |
| 02 | B04 | שמירה עקבית וטיפול בכשלים | `fix(B04): preserve food save outcomes and retry state` | בוצע בקוד; אימות כתיבה חי ממתין |
| 03 | B06 | מניעת שליחה כפולה | `fix(B06): prevent duplicate food submissions` | טרם התחיל |
| 04 | B07 | מניעת כפילות במאגר | `fix(B07): reuse exact catalog matches` | טרם התחיל |
| 05 | B11 | דיוק מד החלבון | `fix(B11): calibrate the protein progress arc` | טרם התחיל |
| 06 | B03 | כמויות בהזנה ידנית | `feat(B03): support quantities in manual food entries` | טרם התחיל |
| 07 | B01 | חזרה בשלבי ההוספה | `fix(B01): preserve drafts when navigating back` | טרם התחיל |
| 08 | B12 | השפעת התיאור על ניתוח צילום | `fix(B12): analyze photos with the current description` | טרם התחיל |
| 09 | B02 | מחיקה ישירה לאחר הוספה | `feat(B02): delete food entries from their immediate context` | טרם התחיל |
| 10 | B10 | העברה בין ארוחות | `feat(B10): move logged food between meals` | טרם התחיל |
| 11 | B05 | הצגת קלוריות מול התפריט | `fix(B05): show calorie intake against the meal plan target` | טרם התחיל |
| 12 | B09 | שילובים עם אבקת חלבון | `feat(B09): add protein powder meal options` | טרם התחיל |
| 13 | B13 | השלמת ערכים ב־AI לפי שם מאכל ומידה | `feat(B13): autofill catalog nutrition using AI` | טרם התחיל |

## משימות התיקון

### קומיט 01 — B08: מאכל חדש לא יירשם כאכילה

**בסיס המחקר:** כפתור מאכל חדש מפעיל את המסלול הידני שמייצר entry. כבר קיימים FoodEditor במצב יצירה ו־saveFood, שכותבים רק למאגר.

**לבצע:** לחבר את כפתור מאכל חדש ל־FoodEditor עם אובייקט חדש; לשמור דרך saveFood. להסיר ממסלול המאגר תאריך, ארוחה ומשמעות של תיעוד אכילה, ולעדכן את הערת הזרימה ב־MyFoods.

**קבלה:**
- [ ] יצירה אחת מוסיפה מאכל אחד ואפס entries, גם לאחר רענון.
- [ ] החלבון, הקלוריות ורשימת היום אינם משתנים.
- [ ] הוספה מהכפתור היומי ממשיכה לתעד אכילה.

**קבצים:** [App.jsx](</Users/udivak/Self Projects/ProCount/src/App.jsx>), [MyFoods.jsx](</Users/udivak/Self Projects/ProCount/src/screens/MyFoods.jsx>), ובמידת הצורך [FoodEditor.jsx](</Users/udivak/Self Projects/ProCount/src/FoodEditor.jsx>). **תלות:** שלב 0.

**תוצאת ביצוע (3.9.2026):** כפתור “מאכל חדש” פותח את `FoodEditor` עם אובייקט חדש, ולכן השמירה עוברת רק דרך `saveFood` ולא דרך `addManual`. בבדיקת UI נפתח העורך ללא שדות תאריך/ארוחה, בלי ליצור רשומה. `npm test` עבר 23/23 ו־`npm run build` עבר. לא בוצעה כתיבת בדיקה ל־Supabase החי; אימות שמירה ורענון מול נתוני בדיקה עדיין ממתין.

### קומיט 02 — B04: שמירה עם תוצאה ברורה וניסיון חוזר בטוח

**בסיס המחקר:** addManual ו־addAi מתעלמים מכשל ברישום וממשיכים לשמירת מאכל; הטפסים נסגרים גם בלי הצלחה. quickAdd אינו ממתין לתשובה.

**לבצע:** לאחד את כתיבת המאגר דרך saveFood ולהחזיר תוצאה ושגיאה מפורשות מהפעולות. להמתין לתוצאה לפני סגירה וניקוי טופס. לשמור את הטיוטה בכשל ואת ה־ID של כל חלק שכבר נשמר; ניסיון חוזר יטפל רק בחלק החסר. לאמת קלט מספרי בגבול פעולת השמירה. במקרה של ניתוק אחרי שליחה, לברר תוצאה לפני יצירה נוספת; אם נדרש לזה מזהה יציב, להשתמש ב־UUID של השורה הקיימת בסכמה ובאותו ID לאורך הניסיון החוזר.

**קבלה:**
- [ ] מאגר בלבד יוצר food אחד; אכילה בלבד יוצרת entry אחד; אכילה עם שמירה יוצרת אחד מכל סוג.
- [ ] כשל לפני שמירה אינו מוחק טיוטה ואינו מייצר הצלחה מדומה.
- [ ] הצלחה חלקית או תשובה שאבדה אינן מובילות ליצירה חוזרת של חלק שכבר נשמר.

**קבצים:** [store.js](</Users/udivak/Self Projects/ProCount/src/store.js>), App, AddSheet, FoodEditor. **תלות:** B08. קומיט זה אינו כולל עדיין נעילת לחיצה כפולה או התאמה למאכל קיים.

**תוצאת ביצוע (3.9.2026):** פעולות המאגר והרישום מחזירות תוצאה מפורשת; כתיבות חדשות משתמשות ב־UUID יציב וב־`insertOrFind` לפני ואחרי כשל תגובה. הטפסים נשארים פתוחים עם הודעת שגיאה, ובעקבות הצלחה חלקית מסמנים את ה־entry שכבר נשמר ומנסים שוב רק את המאכל. בדיקות `node --test src/lib/write.test.js` (4/4), `npm test` (27/27), `npm run build` ו־`git diff --check` עברו. לא בוצעו כתיבות ל־Supabase החי; אימות מול נתוני בדיקה ורענון עדיין ממתין.

### קומיט 03 — B06: לחיצה כפולה יוצרת פעולה אחת

**בסיס המחקר:** שתי שליחות מקבילות של הוספה ידנית או אישור AI שוחזרו כשתי כתיבות.

**לבצע:** להוסיף נעילה סינכרונית לפני ה־await, מצב שומר וכפתור מושבת. הנעילה תכסה את כל השמירה, לרבות שמירה אופציונלית למאגר, ותשתחרר באופן מסודר. להחיל על ידני, מהיר, אישור AI ויצירת מאכל. למנוע מעבר למסלול שמירה חדש דרך אותו טופס בזמן שהפעולה עדיין רצה.

**קבלה:**
- [ ] שתי לחיצות תחת השהיית רשת של שתי שניות יוצרות לכל היותר פעולה אחת בכל מסלול.
- [ ] אחרי כשל ניתן לנסות שוב; אחרי הצלחה אפשר להתחיל הוספה מכוונת נוספת.
- [ ] הוספות מים עצמאיות אינן נחסמות על ידי נעילה גלובלית של כל ה־store.

**קבצים:** App, [AddSheet.jsx](</Users/udivak/Self Projects/ProCount/src/screens/AddSheet.jsx>), FoodEditor. **תלות:** B04.

### קומיט 04 — B07: אותו מאכל לא נוסף שוב למאגר

**בסיס המחקר:** קריאה אחת יוצרת מאכל אחד; קריאות חוזרות יוצרות IDs שונים, ואין בדיקת התאמה. מקור הכפילויות הספציפיות בנתונים החיים עדיין לא אומת.

**לבצע:** לפני יצירת מאכל לבדוק התאמה מלאה של שם מנורמל, מידה, חלבון וקלוריות. להחזיר את המאכל הקיים עם חיווי ברור במקום ליצור שוב. להחיל דרך saveFood המשותף גם על שמירה מתוך ידני ו־AI; בעריכה להחריג את הרשומה הנערכת ולהימנע ממיזוג שקט של שתי רשומות. אין להגדיר ייחודיות לפי שם בלבד.

**קבלה:**
- [ ] שמירת התאמה מלאה פעמיים משאירה מאכל אחד; תיעוד אכילה מכוון נוסף עדיין מותר.
- [ ] אותו שם עם מידה או ערכים שונים נשאר אפשרי.
- [ ] עריכת מאכל ושמירה מתוך AI/ידני אינן עוקפות את הכלל; כפילויות קיימות אינן נמחקות אוטומטית.

**קבצים:** store, App, FoodEditor לפי הצורך. **תלות:** B04, B06. עם גישה חיה, לבדוק IDs וזמני יצירה בקריאה בלבד כדי לאמת את ההסבר לדיווח.

### קומיט 05 — B11: מד חלבון ביחס הנכון

**בסיס המחקר:** קשת באורך כ־270.18 מחושבת לפי 410; לכן 50/150 מצייר כ־50.71%. ringOffset הישן ב־App אינו בשימוש.

**לבצע:** להעביר ל־Today את יחס ההתקדמות המשמש גם לאחוז, לנרמל את הקשת עם pathLength=100 ו־strokeDasharray=100, ולחשב offset של 100×(1−progress). להסיר את ringOffset המת ולמנוע נקודה ירוקה מטעה באפס. להשתמש בערכים מספריים, לא באחוזים ביחידות SVG.

**קבלה:**
- [ ] 0/150 ריק; 50/150 שליש; 75/150 חצי; 100/150 שני שלישים.
- [ ] 150/150 מלא; מעבר ליעד אינו חורג מגבולות הקשת.
- [ ] האחוז והציור עקביים גם לאחר שינוי יעד וניווט ליום קודם.

**קבצים:** [Today.jsx](</Users/udivak/Self Projects/ProCount/src/screens/Today.jsx>), App. **תלות:** שלב 0; ניתן לבצע מוקדם אם תיקון אחר חסום. לבדוק SVG מרונדר ותצוגה באייפון, ולא רק חישוב מספרי. מנגנון הנרמול מתועד ב־[SVG](https://www.w3.org/TR/SVG2/paths.html#PathLengthAttribute).

### קומיט 06 — B03: כמות בהזנה ידנית

**בסיס המחקר:** המידה קיימת אך אין מכפיל כמות; form.grams אינו מוזן בממשק. המסלול המהיר כבר מכפיל ערכים.

**לבצע:** להוסיף כמות ברירת מחדל 1 ותצוגה מקדימה. לשמור ערכי יחידה במאגר וערכי צריכה לאחר הכפלה אחת ביומן. להפעיל את חישוב גרמים ליחידה כשאפשר לפרש אותה, ולאפשר הזנת משקל ידוע בשדה הקיים. כוס או סקופ אינם הופכים לגרמים ללא נתון. לא לשנות את משמעות תוצאת AI, שמייצגת את המנה המצולמת כולה.

**קבלה:**
- [ ] יחידה עם 10 גרם חלבון ו־100 קלוריות בכמות 2 נרשמת כ־20/200; המאגר נשאר 10/100.
- [ ] חצי מנה מחושבת נכון; אפס, שלילי וקלט לא מספרי אינם נשמרים ככמות.
- [ ] יחידת 100 גרם בכמות 2 נרשמת כ־200 גרם; מידה שאינה משקל אינה מקבלת משקל מומצא.

**קבצים:** AddSheet, App, store, ולפי הצורך [nutrition.js](</Users/udivak/Self Projects/ProCount/src/lib/nutrition.js>) ובדיקותיו. **תלות:** B04, B06, B07. המכפיל אינו דורש מיגרציה; תצוגת מספר כפות לאחר רענון, מעבר לגרמים ולסיכומים, תצריך בהמשך שדות מובנים אם תידרש.

### קומיט 07 — B01: חזרה בלי לאבד את הטיוטה

**בסיס המחקר:** חזרה קיימת בכמות בלבד; חזרה ממוצר כללי ומצילום אינה עקבית.

**לבצע:** להגדיר חזרה מכמות לרשימה, ממוצר כללי למסלול שממנו נפתח, ומתוצאת צילום לבחירת תמונה/תיאור. לשמור את הטיוטה במעבר פנימי; לנקות אותה בסגירה מפורשת או בהוספה מוצלחת. לשמור את בחירת היום והארוחה. לסמן תהליך שנסגר כך שתשובת AI מאוחרת לא תחזיר אליו תוצאה.

**קבלה:**
- [ ] חזרה מכל שלב אינה שומרת דבר ואינה מאבדת טקסט או כמות שהוזנו.
- [ ] סגירה ופתיחה מחדש מתחילות תהליך חדש; תשובה מאוחרת אינה ממלאת אותו.
- [ ] הזרימות עובדות ב־RTL וב־PWA באייפון; החזרה הקיימת במסלול המהיר נשמרת.

**קבצים:** App, AddSheet. **תלות:** B03. להשתמש ב־state הקיים; אין צורך להוסיף ספריית ניווט. הסעיף עוסק בכפתורי חזרה בתוך היישום.

### קומיט 08 — B12: ניתוח צילום עם התיאור העדכני

**בסיס המחקר:** התיאור מחובר בקוד המקומי עד לבקשת המודל. אפשר לערוך אותו אחרי שהניתוח התחיל בלי לעדכן את התוצאה. בתיעוד מ־22 באוגוסט נכתב שפונקציית השרת לא פורסמה באותה עבודה; מצב הפרסום כיום והשפעת הטקסט על המודל לא אומתו.

**לבצע לפי הסדר:**
1. לאמת שה־PWA פונה לפרויקט הנכון ושהפונקציה המפורסמת מכילה את guidance ואת SYSTEM_PROMPT. אם גישה חיה עדיין חסרה, להשלים את הבדיקה המקומית ולתעד את החלק שלא אומת.
2. לבדוק עם בקשות מדומות שהטקסט בזמן הלחיצה מופיע בגוף הבקשה היוצאת למודל לצד התמונה הנכונה. להוסיף בדיקת חוזה עבור טקסט, טקסט ריק ומגבלת האורך.
3. לנעול עריכה בזמן בקשה. לאחר שינוי התיאור בתוצאה קיימת, לסמן שהיא אינה מעודכנת ולהציע נתח מחדש עם התיאור המעודכן. לשייך כל תוצאה לגרסת התמונה והטקסט שלה, ולהיעזר בהגנה על תהליך סגור מ־B01.
4. לבצע בסביבת בדיקה שלושה זוגות ניתוח של אותה תמונה, בלי ועם תיאור משמעותי: משקל ידוע, רכיב שקשה לראות ותיקון זיהוי. לבדוק השפעה על זיהוי, הנחות וחישוב; חזרה מילולית על התיאור לבדה אינה הוכחה.
5. לחדד את הוראות המודל רק אם הטקסט מגיע ובכל זאת אינו משפיע כנדרש. להגדיר שימוש במידע מפורש על מרכיבים וכמויות, ולציין סתירות בהערה הקיימת תוך שמירת כללי הפלט. שינוי בקוד פונקציית השרת ייכלל באותו קומיט B12; פרסום ואימות חי יתועדו בנפרד.

**קבלה:**
- [ ] בקשת המודל כוללת את התיאור והתמונה העדכניים; טקסט ריק ומגבלת 1,000 תווים מטופלים כראוי.
- [ ] שינוי תיאור אינו נראה כאילו כבר השפיע; ניתוח מחדש מפורש מעדכן את התוצאה, ובקשה ישנה אינה דורסת חדשה.
- [ ] נתונים מהותיים משתקפים באומדן או שהסתירה מוסברת. נשמרים ניתוח ללא תיאור, מכסת AI ושמירה חד־פעמית ליומן.

**קבצים:** App, AddSheet, store, [פונקציית הניתוח](</Users/udivak/Self Projects/ProCount/supabase/functions/analyze-food-photo/index.ts>), בדיקת חוזה לצדה ו־[תיעוד השרת](</Users/udivak/Self Projects/ProCount/supabase/README.md>) אם החוזה או ההוראות השתנו. **תלות:** B04, B06, B01. בירור הגרסה החיה יכול להתחיל כבר בשלב 0.

**אימות שרת:** `deno test supabase/functions/analyze-food-photo/`. אם Deno אינו זמין, להריץ בסביבה מתאימה לפני סגירת הסעיף. אין לסמן בדיקת מודל חיה או פרסום כהושלמו על סמך בדיקה מדומה. ההשוואות מכבדות את המכסה ואינן נרשמות כאכילה.

### קומיט 09 — B02: מחיקה מהירה במקום שבו עובדים

**בסיס המחקר:** מחיקה קיימת ברשימת היום בלבד; חלונית הפרטים לקריאה בלבד ואין ביטול להוספה האחרונה.

**לבצע:** לשמור את ה־ID של רישום המזון האחרון שהתווסף בהצלחה ולהציג פעולת מחיקה ישירה בהודעת ההצלחה, גם כשהטופס נסגר. להוסיף מחיקה בחלונית הפרטים. שתי הפעולות יעברו דרך ConfirmDialog. להחזיר שגיאת מחיקה ולתקן רק את מצב הרשומה שנמחקה אם הכתיבה נכשלה, בלי להוסיף שכבת rollback כללית או לשנות פעולות פרופיל.

**קבלה:**
- [ ] ניתן למחוק הוספה ידנית, מהירה או AI בלי לחפש ברשימת היום; רק ה־ID שנבחר נמחק.
- [ ] מאכל שנשמר במאגר נשאר בו; ביטול אישור אינו מוחק דבר.
- [ ] כשל מחיקה אינו מוצג כהצלחה, הסיכומים עקביים לאחר רענון, וביטול הוספת מים ממשיך לעבוד.

**קבצים:** App, [ItemDetailModal.jsx](</Users/udivak/Self Projects/ProCount/src/ItemDetailModal.jsx>), store. **תלות:** B04, B06. להשתמש בתבנית החיווי הקיימת למים בלי לשתף בין הפעולות את ה־ID לביטול.

### קומיט 10 — B10: העברת רישום בין ארוחות

**בסיס המחקר:** meal_type כבר קיים ב־entries; אין פעולת עדכון בממשק או ב־store.

**לבצע:** להוסיף בחירת אחת מארבע הארוחות בחלונית הפרטים. לעדכן meal_type לפי ID בשכבת הנתונים ולהציג את השורה שהשרת החזיר. לעדכן או לסגור את פרטי הרשומה כדי שלא יישאר בהם snapshot ישן.

**קבלה:**
- [ ] העברה מצהריים לערב משנה את הקבוצה ונשמרת לאחר רענון.
- [ ] ID, תאריך, מקור, כמות, חלבון, קלוריות ומספר הרישומים אינם משתנים.
- [ ] כשל שרת משאיר את הארוחה הקודמת ומציג שגיאה.

**קבצים:** ItemDetailModal, App, store. **תלות:** B04, B02 בשל השינויים באותה חלונית. אין צורך במיגרציה או בגרירה ושחרור.

### קומיט 11 — B05: קלוריות מול יעד התפריט

**בסיס המחקר:** הבית והתפריט כבר משתמשים ב־2,250, אך בבית מוצג מספר רץ בלי יעד גלוי. הסעיף יבוצע כהבהרת הצריכה מול התפריט הקיים.

**לבצע:** לשים את יעד הקלוריות הקבוע במקור אחד ולהשתמש בו בבית ובתפריט. להציג נאכלו X מתוך 2,250, יתרה או חריגה. להשאיר את התפריט כהנחיה סטטית ואת סכומי האכילה כנתונים בפועל.

**קבלה:**
- [ ] 1,125 מתוך 2,250 מוצגים כחצי ונשארות 1,125 קלוריות.
- [ ] חריגה מוצגת במספר גם כאשר הבר מלא.
- [ ] הוספה, מחיקה ויום קודם מציגים את הסיכום הנכון מול אותו יעד.

**קבצים:** App, Today, [MealPlan.jsx](</Users/udivak/Self Projects/ProCount/src/screens/MealPlan.jsx>), וקבוע משותף במודול קיים לפי הצורך. **תלות:** B11 עקב התצוגה המשותפת. יעד ניתן לעריכה ותפריט דינמי אינם נדרשים לתיקון זה.

### קומיט 12 — B09: שילובים עם אבקת חלבון בתפריט

**בסיס המחקר:** השילובים אינם קיימים בתפריט; זו תוספת תוכן ואינה דורשת שינוי במנוע ההמלצות הביתי.

**לבצע:** להוסיף שלושה שילובים קצרים עם אופן הכנה וכמות ברורה. להשתמש במנת היצרן לאבקה; לפני הצגת גרמים, חלבון וקלוריות מדויקים לאמת את תווית המוצר והמרכיבים. אם התווית עדיין אינה זמינה, אפשר להכין את התוכן ללא סכומים תזונתיים לא מאומתים ולתעד מה נותר להשלמה.

**קבלה:**
- [ ] השילובים קריאים באייפון ומציינים איזו כמות מכל מרכיב נדרשת.
- [ ] כל ערך מספרי תזונתי שמוצג מבוסס על נתון מאומת, כולל גודל מנת האבקה.
- [ ] צפייה בהמלצה אינה מוסיפה food או entry ואינה משנה את התפריט הקיים מעבר לתוספת המבוקשת.

**קבצים:** MealPlan. **תלות:** פרטי המוצר לצורך הערכים המדויקים בלבד; חוסר בהם אינו מעכב את יתר התיקונים.

### קומיט 13 — B13: השלמת חלבון וקלוריות ב־AI מתוך יצירת מאכל

**הצורך:** להזין שם מאכל ולבחור את המידה המקובלת שלו — יחידה, 100 גרם, כף, כוס, סקופ או מידה מותאמת — ובלחיצה אחת לקבל את החלבון והקלוריות ישירות לשדות הטופס. הפעולה תחסוך יציאה מהאפליקציה, שאלה ידנית ל־AI והעתקת התשובה בחזרה.

**בסיס המחקר:** ב־FoodEditor כבר קיימים שדות שם, מידה, חלבון וקלוריות, אך אין בו בקשת AI. פונקציית analyze-food-photo כבר כוללת אימות משתמש, מכסה, קריאה למודל ותשובה מובנית; כרגע היא מחייבת תמונה וההוראות שלה מתייחסות לתמונה בלבד. לכן נדרשת הרחבה מפורשת למצב טקסט, ולא רק הפעלת קריאת הצילום ללא קובץ.

**לבצע לפי הסדר:**
1. להוסיף במסך מאכל חדש כפתור **השלם ערכים עם AI**, שזמין לאחר הזנת שם ובחירת מידה תקינה. לשלוח את השם והמידה המדויקים; לציין בממשק שאפשר לדייק את השם עם מותג או מצב הכנה, למשל מבושל/נא. אין צורך לצרף תמונה.
2. להעביר callback מ־App ל־FoodEditor; פעולת הרשת עצמה תישאר ב־store. להרחיב את פונקציית השרת הקיימת במצב טקסט מפורש, למשל `mode: "text"` עם `foodName` ו־`unit`. בקשות הצילום הקיימות ימשיכו לעבוד ללא שינוי, עם מצב צילום כברירת מחדל. לכל מצב תהיה ולידציה מתאימה; בקשת טקסט לא תחויב בתמונה ובקשת צילום עדיין תחויב בה.
3. להשתמש באותו ספק, מודל, API שרתי ומפתח מאובטח של הצילום. להתאים את הוראות מצב הטקסט להחזרת חלבון וקלוריות **ליחידה אחת מהמידה שנבחרה** ולשמר את הפלט המובנה והוולידציה. להציג בהערה הנחות כגון גודל יחידה או משקל כף כשאינם ידועים. אין להמציא מותג, משקל מדויק או מקור תזונתי.
4. להפעיל את אותה מכסת AI שרתית המשותפת לצילום, כיום 6 קריאות ביום. לציין שהפעולה משתמשת בקריאת AI, לחסום לחיצות מקבילות ולהציג מצב טעינה מיידי. אין להוסיף קריאה נוספת בכל הקלדה או לשנות את המכסה ללא צורך.
5. בהצלחה, למלא את שדות החלבון והקלוריות, להשאירם ניתנים לעריכה ולהציג את המידה וההנחות שעליהן מבוססת התוצאה. לשייך את התוצאה לשם ולמידה שנשלחו; שינוי שלהם או סגירת הטופס בזמן בקשה לא יאפשרו לתשובה ישנה לדרוס טופס אחר או תיקונים ידניים. אין לשנות את המידה שנבחרה בשקט.
6. בחוסר מידע, כשל רשת או מיצוי מכסה, להשאיר את הנתונים שכבר הוזנו ולהציג הודעה עם אפשרות להמשך ידני. מילוי הערכים הוא שלב בטופס בלבד: רק לחיצה על הוסף מאכל תפעיל את מסלול השמירה למאגר מ־B08, בלי יצירת רישום אכילה.

**דיוק התוצאה:** ללא תווית או מקור תזונתי מאומת, תשובת המודל היא אומדן, גם כשהיא מובנית ומהירה. להציג חיווי קצר של אומדן AI ולא הבטחת דיוק מוחלט. בדיקת האיכות תשווה מזונות ויחידות מוכרים לנתוני ייחוס מאומתים; חיווי ביטחון של המודל לבדו אינו אימות. אם השם או גודל המנה עמומים, לבקש פירוט מתאים בטופס או להציג את ההנחה בבירור.

**קבלה:**
- [ ] שם ומידה → לחיצה אחת → חלבון וקלוריות ממולאים עבור המידה שנבחרה, ללא צילום וללא יציאה מהאפליקציה. לבדוק לפחות יחידה, 100 גרם וכף, וכן מידה מותאמת ונתונים עמומים.
- [ ] בקשה אחת לכל לחיצה מכוונת, שימוש במכסה המשותפת, ערכים סופיים מספריים ואי־שליליים, אפשרות תיקון ידני ושמירת הטיוטה בכשל. תשובה ישנה אינה דורסת שינוי בשם, במידה או בערכים.
- [ ] מילוי אוטומטי אינו כותב ל־foods או ל־entries; שמירה לאחריו יוצרת מאכל אחד ואפס entries. ניתוח צילום עם ובלי תיאור נשאר תקין אחרי הרחבת הפונקציה.

**קבצים:** [FoodEditor.jsx](</Users/udivak/Self Projects/ProCount/src/FoodEditor.jsx>), [App.jsx](</Users/udivak/Self Projects/ProCount/src/App.jsx>), [store.js](</Users/udivak/Self Projects/ProCount/src/store.js>), [פונקציית הניתוח](</Users/udivak/Self Projects/ProCount/supabase/functions/analyze-food-photo/index.ts>), בדיקות החוזה והוולידציה לצדה ו־[תיעוד השרת](</Users/udivak/Self Projects/ProCount/supabase/README.md>). **תלות:** B08, B04, B06, B07 ו־B12. אין צורך בשינוי סכמת המאגר או במנוע AI נוסף.

**אימות:** בדיקות חוזה מדומות לשני מצבי הבקשה, בדיקות Deno, בדיקת מילוי/עריכה/שמירה במסך ובדיקת איכות קטנה מול נתוני ייחוס עם יחידות מוגדרות. למדוד את זמן ההמתנה בבדיקה; אין לקבוע הבטחת מהירות לפני מדידה. לאחר פרסום הפונקציה לבדוק גם את שני המצבים החיּים ולתעד את גרסת השרת. הסעיף נוסף לתכנית בלבד; לא בוצעה קריאת AI או בדיקת איכות חיה במסגרת העדכון הזה.

## בדיקה וסגירת כל קומיט

1. לשחזר את המקרה הרלוונטי לפני השינוי על ענף התיקונים, ולרשום את התוצאה.
2. לבצע את התיקון ולבדוק את תרחישי הקבלה שלו. ללוגיקת כתיבה וכמויות להוסיף בדיקה חוזרת ממוקדת מול נתונים מדומים; לציור ולניווט לבדוק גם את הממשק בפועל.
3. להריץ את הבדיקות והבנייה. לשינוי Edge Function להריץ גם בדיקות Deno; לשינוי UI לבדוק רוחב אייפון ו־RTL.

```sh
npm test
npm run build
git diff --check
git diff --stat
```

4. לעדכן את מצב B הרלוונטי במסמך עם הבדיקה שבוצעה. לבצע stage רק לקבצי התיקון, לבדיקה שלו ולעדכון התכנית; לבדוק את ה־diff המבוים לפני commit.

```sh
git diff --cached
git diff --cached --check
```

5. ליצור קומיט עם ההודעה ממפת הקומיטים ולוודא שהוא מכיל מזהה B אחד בלבד. לא לצרף שינוי לא קשור כדי להגיע לקומיט נקי.

## נקודות בקרה

- [ ] **אחרי קומיט 04:** שלושת מסלולי השמירה כותבים לטבלאות הצפויות; לחיצה כפולה, שמירה חוזרת וכשל חלקי אינם מכפילים נתונים.
- [ ] **אחרי קומיט 06:** המד משקף את השבר; הכמות משנה צריכה ולא את ערכי הבסיס במאגר.
- [ ] **אחרי קומיט 08:** חזרה שומרת טיוטה; כל תוצאת צילום שייכת לתיאור הנכון. לציין בנפרד אם בדיקת המודל החיה עדיין ממתינה.
- [ ] **אחרי קומיט 10:** מחיקה והעברת ארוחה אינן פוגעות בתאריך, בסיכומים, במאגר או במעקב המים.
- [ ] **אחרי קומיט 12:** תצוגת הקלוריות ותוספת התוכן נבדקו; נתונים תזונתיים שלא אומתו אינם מוצגים כמספרים מדויקים.
- [ ] **אחרי קומיט 13:** מילוי הערכים ב־AI עובד מתוך מאכל חדש, השמירה נותרת למאגר בלבד וניתוח הצילום אינו נפגע. כל 13 הסעיפים מתועדים, והבדיקות/הבנייה עוברות ללא רגרסיה לעומת baseline של שלב 0.

## מסירה ופרסום

- [ ] להכין רשימת B → hash → תוצאה, ולפרט כל בדיקה שלא בוצעה. אין לסמן חשד שנבדק רק בקוד כהתנהגות שאומתה מול המודל החי.
- [ ] לבצע סבב PWA באייפון: הוספה ידנית/מהירה/AI, מילוי ערכים לפי שם ומידה במאגר, יום קודם, חזרה, רענון, רשת איטית וכשל שמירה.
- [ ] לקרוא מחדש את היסטוריית הקומיטים מול הבסיס ולוודא שכל תיקון נשמר בנפרד:

```sh
git log --reverse --oneline origin/master..HEAD
git diff --stat origin/master...HEAD
git status --short
```

- [ ] להכין תיאור מסירה/PR שמסכם את 13 המזהים ואת האימות. דחיפה, מיזוג ופרסום יתבצעו במסגרת בקשת הביצוע וההרשאה המתאימה; הכנת מסמך זה אינה מבצעת אותם.
- [ ] B13 מחייב פרסום של הרחבת ה־Edge Function; B12 עשוי לחייב גם שינוי בהוראות או בגרסה המפורסמת. לתעד איזה קומיט פורסם ולבדוק את הבקשה והתשובה בשני מצבי הקלט לאחר הפרסום. פרסום frontend אינו הוכחה לפרסום הפונקציה.
- [ ] לא למחוק כפילויות היסטוריות כחלק אוטומטי מהתיקונים. אם נמצאו, להציג רשומות מדויקות ותכנית ניקוי נפרדת.

## ראיות המחקר שנשמרות כנקודת מוצא

| ממצא קודם | רמת האימות |
|---|---|
| addManual/addAi עם שמירה: פעולה אחת יוצרת entry אחד ו־food אחד; שתי פעולות במקביל יוצרות שניים מכל סוג | שוחזר מול תחליף Supabase בזיכרון; לא בייצור |
| saveFood במצב חדש יוצר food ללא entry | שוחזר מקומית |
| כשל entry עדיין מאפשר שמירת food ואינו מוחזר מ־addManual | שוחזר מקומית |
| ידני מתעלם ממכפיל כמות; מהיר מכפיל ערכים וגרמים | שוחזר מקומית |
| 50/150 מצייר כ־50.71% מאורך הקשת | חישוב גאומטרי מול ה־SVG הנוכחי |
| guidance מחובר מה־textarea עד לבקשת המודל; שינוי מאוחר אינו מנתח מחדש | נבדק בקוד המקומי |
| פונקציית ההכוונה לא פורסמה בעת פיתוחה ב־22 באוגוסט | תיעוד היסטורי; עשוי להיות לא עדכני |
| FoodEditor אינו משלים ערכים עם AI; פונקציית הניתוח מחייבת תמונה ואינה כוללת מצב שם ומידה | נבדק בקוד המקומי בעת הוספת B13; לא נבדק מול שרת חי |

הבדיקות הקודמות עברו 23/23 והבנייה הצליחה בענף המחקר. חיבור Supabase דחה קריאה לפרויקט מחוסר הרשאה; הכפילויות החיות, גרסת פונקציית הצילום והשפעת התיאור על המודל עדיין לא אומתו. את הנתונים האלה יש לרענן לפי שלב 0 ו־B12.

סקריפט השחזור הזמני, אם עדיין זמין: [reproduce.mjs](/private/tmp/procount-bug-research.H5ROaA/reproduce.mjs). הוא מתעד את ההתנהגות הלקויה ואינו בדיקת קבלה לתיקון; הבדיקות החדשות צריכות לצפות להתנהגות המתוקנת.

התכנית אורגנה בעזרת planning-and-task-breakdown: מזהה קבוע, קומיט, תלות ותנאי קבלה לכל סעיף. אין תלות בסקריפט זמני כדי להתחיל את הביצוע.
